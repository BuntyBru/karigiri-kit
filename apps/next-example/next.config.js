/** @type {import('next').NextConfig} */
const nextConfig = {
  transpilePackages: [
    '@karigiri-kit/react',
    '@karigiri-kit/react-theme',
    '@karigiri-kit/tokens',
  ],
}

module.exports = nextConfig 